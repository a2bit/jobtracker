apiVersion: v1
kind: Service
metadata:
  name: jobtracker
  namespace: jobtracker
spec:
  selector:
    app.kubernetes.io/name: jobtracker
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
